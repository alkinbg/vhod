# Phase 9 final hardening execution note

This branch is intentionally test-first.

1. Expand schema guarantees for all Phase 9 entities and critical constraints.
2. Add the missing assembly + representative proxy lookup index only after RED confirms the gap.
3. Expand management role/CSRF regression coverage.
4. Expand post-finalization immutability regression coverage.
5. Require exact-head full CI before merge.
